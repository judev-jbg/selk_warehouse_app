import 'package:flutter/material.dart';

class AppColors {
  // Color principal de la marca Selk
  static const Color primary = Color(0xFFC10556);

  // Variaciones del color principal
  static const Color primaryLight = Color(0xFFE5568B);
  static const Color primaryDark = Color(0xFF8A0039);

  // Colores complementarios (basados en el color principal)
  static const Color secondary = Color(0xFF05C156);
  static const Color accent = Color(0xFF0556C1);

  // Colores de estado
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFC107);
  static const Color error = Color(0xFFF44336);
  static const Color info = Color(0xFF2196F3);

  // Tonos neutros
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color onSurface = Color(0xFF212121);
  static const Color divider = Color(0xFFBDBDBD);
}
